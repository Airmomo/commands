---
description: 并行诊断调试 - 启动多个独立调查 Agent 同时从不同方向排查错误根因，竞速验证修复方案
argument-hint: <error-message>
allowed-tools: Agent, Bash, Read, Edit, Glob, Grep, mcp__context7__query-docs, mcp__context7__resolve-library-id, mcp__zread__search_doc, mcp__zread__read_file, mcp__zweb-search__web_search_prime
---

# 并行诊断调试

你收到用户报告的错误：`$ARGUMENTS`

## 约束规则（必须严格遵守）

- **禁止串行排查**：三个 Agent 必须并行启动，不允许逐个执行
- **禁止询问用户选择**：你自己判断哪个修复方案有效，直接应用验证通过的修复
- **禁止只报告不修复**：必须实际应用修复，而不是列出建议让用户选择
- **禁止创建额外文件**：只修复代码问题，不创建调试脚本、日志或报告文件
- **只修复根因**：应用通过验证的那一个修复方案，不做额外改动

## 参数验证

如果 `$ARGUMENTS` 为空，直接告知用户正确用法并停止：

```
/parallel-debug <error-message>
```

## 执行流程

### 阶段 1：并行调查（竞速模式）

使用 `Agent` 工具同时启动 **3 个调查 Agent**，每个 Agent 持有不同的根因假设。所有 Agent 必须在 **同一条消息中并行调用**。

**Agent A — 配置结构检查**

- 假设：错误源于配置文件的结构问题（JSON/YAML/TOML 键名错误、缺少必需字段、格式错误）
- 操作：
  1. 读取项目中的配置文件（package.json、tsconfig.json、.env、docker-compose.yml、settings.json 等）
  2. 检查结构完整性、字段有效性、版本兼容性
  3. 如发现配置问题，提出修复方案并在隔离测试中验证
- 返回：验证结果 + 修复方案（如有）

**Agent B — 运行时逻辑追踪**

- 假设：错误源于代码执行路径中的逻辑错误（类型错误、空指针、条件判断、异步问题等）
- 操作：
  1. 根据错误信息中的堆栈追踪定位相关源代码文件
  2. 读取并分析相关代码的执行路径
  3. 使用 Bash 工具运行隔离测试（如 `node -e`、`python -c` 等最小复现）验证假设
  4. 如发现逻辑错误，提出修复方案并测试
- 返回：验证结果 + 修复方案（如有）

**Agent C — 文档与版本检查**

- 假设：错误源于版本不匹配、API 变更或已知问题
- 操作：
  1. 从错误信息中识别相关库/框架/工具
  2. 使用 `mcp__context7__resolve-library-id` 和 `mcp__context7__query-docs` 查询最新文档
  3. 使用 `mcp__zread__search_doc` 搜索相关仓库的 issues 和文档
  4. 使用 `mcp__zweb-search__web_search_prime` 联网搜索该错误的社区讨论、Stack Overflow 解答和已知 issue
  5. 比对项目使用的版本与文档要求的版本
  6. 如发现版本或兼容性问题，提出修复方案
- 返回：验证结果 + 修复方案（如有）

### 阶段 2：裁决与修复

收到所有 Agent 的调查结果后：

1. **评估每个修复方案**：检查哪个 Agent 的根因假设被验证通过
2. **选择验证通过的修复**：只采纳通过了隔离测试的方案
3. **应用修复**：使用 Edit 工具直接修改代码
4. **验证修复**：通过 Bash 运行测试或最小复现命令确认错误已消除

如果多个 Agent 都找到了有效的修复：
- 优先选择修复根因的方案而非绕行方案
- 如果都修复根因，选择改动范围最小的方案

如果所有 Agent 都未找到有效修复：
- 汇报三个方向的调查结果
- 说明各自的发现和排除依据
- 提出下一步排查建议

### 阶段 3：输出结果

仅输出以下格式的结果：

```
🔍 错误: [原始错误摘要]
✅ 根因: [哪个 Agent 找到的 + 根因描述]
🔧 修复: [具体改动了什么]
✨ 验证: [验证命令及结果]

❌ 排除: [其他 Agent 排除了什么可能性]
```
