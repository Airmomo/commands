---
description: 根据 command-development.md 规范检查指定的命令文件，查漏补缺并修复问题。
argument-hint: <command-file>
allowed-tools: Read, Edit, Glob, Grep, Bash
---

# 命令规范检查

检查命令文件 `$ARGUMENTS` 是否符合 `@docs/command-development.md` 规范，发现问题后直接修复。

## 约束规则

- **只修复目标文件**：不要创建新文件、备份文件或辅助脚本
- **不要破坏已有功能**：修复规范问题的同时保持命令的核心逻辑和参数不变
- **直接修复**：发现问题后用 Edit 工具直接修正，不要只列出问题等你确认

## 执行步骤

### 1. 读取规范文档

读取 `@docs/command-development.md` 获取完整规范。

### 2. 读取目标命令

读取 `$ARGUMENTS` 指定的命令文件。

### 3. 逐项检查

按以下检查清单逐项比对，记录不符合的项目：

| # | 检查项 | 规范要求 |
|---|--------|----------|
| 1 | 文件格式 | `.md` 文件，头部包含 YAML frontmatter |
| 2 | `description` | frontmatter 中存在，描述清晰简洁 |
| 3 | `argument-hint` | frontmatter 中存在（如有参数） |
| 4 | `allowed-tools` | frontmatter 中已配置，且使用最小权限原则 |
| 5 | `model` | 如有特殊模型需求应在 frontmatter 中指定 |
| 6 | 动态参数 | `$ARGUMENTS` 或 `$1`/`$2`/`$3` 使用正确 |
| 7 | 文件引用 | `@` 语法引用是否正确（如有使用） |
| 8 | Bash 执行 | `` !` `` 语法用于注入上下文，`allowed-tools: Bash` 用于动态执行，两者不混淆 |
| 9 | Bash 模板语法 | 代码块中的 bash 命令语法合法，占位符清晰标注 |
| 10 | 错误处理 | 参数缺失、输入无效等边界情况有处理 |
| 11 | 注释 | 复杂逻辑有必要的注释说明 |
| 12 | 单一职责 | 命令只做一件事，职责明确 |

### 4. 修复问题

对每个不符合的检查项，直接用 Edit 工具修复目标文件。修复时：

- 保持命令原有的核心功能和输入参数不变
- 不要添加用户未要求的新功能
- 不要重写整个文件，只修改需要修正的部分

### 5. 输出检查报告

修复完成后，输出简短的检查结果：

```
✅ 通过: [列出通过的检查项]
🔧 已修复: [列出修复的问题及改动摘要]
⚠️ 需人工确认: [列出无法自动修复或需用户决策的问题，如有]
```
